The test file uses table-driven testing to cover the three required scenarios: exact match, complete inversion (full uint64 mask), and a mid-range partial mismatch. Float comparison uses a tight epsilon (1e-9) via math.Abs to safely assert similarity against expected values without floating-point precision drift. The test assumes the HammingDistance(a, b uint64) int and Similarity(a, b uint64) float64 function signatures established in task-1. It relies on standard library only and requires no external fixtures.